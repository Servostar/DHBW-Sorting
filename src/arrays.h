#ifndef _ARRAYS_H_
#define _ARRAYS_H_

#include <stdbool.h>
#include <stdio.h>
#include <stdlib.h>

void populate(int *, int);

void shuffle(int *, int);

void swap(int *, int *);

bool is_sorted(int *, int);

void print_array(int *, int);

#endif
